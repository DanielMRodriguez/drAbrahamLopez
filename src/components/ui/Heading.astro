---
interface Props {
	variant?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5';
	class?: string;
}

const { variant = 'h2', class: className } = Astro.props;
const Tag = variant;
---

<Tag class={className}>
	<slot />
</Tag>
